import { MiniguiComponent } from '../component';

export declare class ButtonGroup extends MiniguiComponent {
  /**
   * can be set to small large or omitted
   * @default 'default'
   * @type string
   */
  size: 'small' | 'large' | 'default';
}
